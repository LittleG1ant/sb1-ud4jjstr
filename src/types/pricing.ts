export type PricingTier = {
  duration: string;
  athletes: {
    one: string;
    two: string;
    three: string;
    four: string;
  };
};

export type TrainingFeature = {
  icon: string;
  title: string;
  description: string;
};