export type ContactFormData = {
  name: string;
  email: string;
  phone?: string;
  message: string;
};

export type ContactInfo = {
  icon: string;
  label: string;
  value: string;
  link?: string;
};